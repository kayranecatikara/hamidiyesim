# ============ Python ============
__pycache__/
*.py[cod]
*.egg-info/

# ============ Sanal ortam (yerelde kalır, repoya girmez) ============
.venv/
venv/
env/

# ============ Mission Planner binary (~400MB) ============
# 'bash scripts/setup_mission_planner.sh' ile indirilir — repoda tutulmaz.
tools/mission_planner/

# ============ Çalışma çıktıları ============
logs/
*.log
*.tlog
*.BIN
*.bak

# ============ ArduPilot SITL geçici dosyaları ============
eeprom.bin
mav.parm
*.parm.bak

# ============ Editör / OS ============
*.swp
.vscode/
.idea/
.DS_Store

# ============ YOLO detection (üretilen veri + ağırlıklar) ============
vision/datasets/
vision/models/*
runs/
*.pt
# Eğitilmiş güncel model repoya girer (5.5MB) — dataset girmez
!vision/models/avci_yolo.pt
!vision/models/avci_pose.pt
.claude/*
!.claude/skills/
logs/visual_lead_*.csv

# ============ Eğitim çıktıları (ağırlıklar vision/models altında tutulur) ============
vision/runs_pose/
vision/runs/

# Talon mesh ölçekleme yedeği (13 MB) — orijinaller git tarihçesinde zaten var
sim/gazebo_harmonic/models/_mesh_yedek/
