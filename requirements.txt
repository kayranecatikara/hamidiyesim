# AVCI SİM — Python (pip) bağımlılıkları
# ---------------------------------------------------------------
# Kurulum:  pip install -r requirements.txt
#
# NOT: Aşağıdakiler pip ile DEĞİL, apt ile gelir (README'ye bakın):
#   - rclpy, cv_bridge, sensor_msgs, gazebo_msgs  -> ROS 2 Humble
#   - gz.transport13 / gz.msgs10                  -> python3-gz-transport13
# ---------------------------------------------------------------
opencv-python>=4.5
numpy>=1.21
fastapi>=0.100
uvicorn>=0.20
pydantic>=2.0
pymavlink>=2.4
ultralytics>=8.3   # YOLO detection (eğitim + çıkarım); torch ayrıca gerekli (CUDA'lı)
boxmot==19.0.0     # HybridSORT takip (vision/tracker.py). SÜRÜM SABİT: 20+ numpy>=2.2
                   # dayatır, numpy 2 buradaki cv2 4.9 / torch 2.5.1 kurulumunu bozar.
scipy>=1.10        # boxmot→filterpy bağımlılığı. apt'nin scipy 1.8'i numpy<1.25
                   # istiyor ve her çalıştırmada UserWarning basıyor; pip'ten
                   # 1.13.1 kurulunca uyarı biter (numpy 1.26 korunur).
