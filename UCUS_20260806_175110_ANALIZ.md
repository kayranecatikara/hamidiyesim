# Uçuş 20260806_175110 — analiz + entegrasyon karşılaştırması

Bu belge üç soruyu cevaplar:

1. Neden düz uçuşta eskiden tek geçişte vuruyordu, şimdi 5. geçişte?
2. Vurduktan sonra hedef neden düşmüyor?
3. Benim halimde ne vardı, Kayra'nın dalından hangi değerler alındı?

> ⚠ Bu uçuş **entegrasyondan ÖNCEKİ** kodla yapıldı (17:51). GPS entegrasyonu
> 20:00 civarı commit edildi (`c6869f2`). Yani aşağıdaki 12 fazlık sonuç
> **yeni GPS güdümünün karnesi değil**, ondan önceki halin karnesi.

---

## 1. Uçuşun karnesi

```
[GPS] 2647 kare
  istasyonda oturma (d_h 8-12 m): 25.3%   min d_h: 1.1 m
  kadraj @KILIT: yaw RMS 69.3°  elev ort 9.1° (istasyon 15°)
  dikey açık: max 20.4 m  son 4.7 m
[GÖRSEL] 12 faz | kalite oranı 76% | yaw RMS 24.1° | en yakın 0.33 m | VURULDU ✓
```

| faz | kare | süre | devir→en yakın→son | sonuç |
|---|---:|---:|---|---|
| 175111 | 111 | 3.6 s | 20.8 → 20.76 → 62.3 | kayıp |
| 175128 | 103 | 3.4 s | 20.4 → **0.37** → 2.0 | ıska |
| 175135 | 35 | 1.1 s | 10.1 → 1.48 → 3.1 | ıska |
| **175227** | **297** | **9.8 s** | 11.7 → **0.47** → 0.5 | **VURULDU** |
| 175330 | 78 | 2.5 s | 17.2 → 17.18 → 51.2 | kayıp |
| 175350 | 121 | 4.0 s | 19.9 → **0.46** → 1.9 | ıska |
| 175355 | 88 | 2.9 s | 15.2 → 2.54 → 4.1 | kayıp |
| 175419 | 113 | 3.7 s | 20.5 → 1.16 → 2.7 | ıska |
| 175426 | 17 | 0.5 s | 3.7 → 1.26 → 3.1 | ıska |
| 175427 | 88 | 2.9 s | 14.3 → 14.29 → 50.0 | kayıp |
| 175439 | 109 | 3.6 s | 19.7 → **0.33** → 2.0 | ıska |
| 175446 | 45 | 1.5 s | 12.2 → 1.45 → 3.4 | ıska |

---

## 2. "Neden 5. geçişte vuruyor?" — ÖLÇÜLDÜ

### Bulgu: menzil vuruşu belirlemiyor, GEOMETRİ belirliyor

En yakın dört geçişin son karelerini yan yana koyun:

| faz | en yakın | kapanma hızı (m/s) | sonuç |
|---|---:|---:|---|
| 175439 | **0.33 m** | 6.85 → 6.5 → 5.41 → 2.06 | **ıska** |
| 175128 | **0.37 m** | 7.05 → 6.76 → 6.01 → 3.85 | **ıska** |
| 175350 | **0.46 m** | 0.68 → 0.61 → 0.24 → 0.27 | **ıska** |
| 175227 | **0.47 m** | 7.75 → 7.78 → 7.78 → 3.87 | **VURULDU ✓** |

**0.33 m'de temas yok, 0.47 m'de var.** Menzil merkez–merkez mesafesidir;
Talon'un kanat açıklığı ~1.4 m, gövde çapı ~0.15 m. 0.33 m merkez mesafesi
"gövdenin 33 cm yanından geçtim" demek olabilir — temas sensörü haklı olarak
susuyor. Vuruş için merkezi değil **gövde/kanat yüzeyini** kesmek gerekiyor.

Bu zaten kodda yazılıydı (`visual_lead._vurus_oldu`):
> *"0.61 m'de bile Gazebo temas sensörü tetiklenmiyor (ölçüldü) — gerçek temas
> için ~0.3 m daha kapatmak gerekiyor."*

### Vuran faz neyiyle farklıydı

| | vuran (175227) | ıskalayan 11 faz (medyan) |
|---|---:|---:|
| süre | **9.8 s** | 2.9 s |
| kare | **297** | 88 |
| \|yaw hatası\| medyanı | **0.3°** | 2.8-64.1° |
| kör dalış karesi | **0** | 5-19 |
| devir menzili | 11.7 m | 15.2-20.5 m |

Vuran faz **3.4 kat uzun** yaşadı ve boyunca burnu hedefteydi (yaw hatası
medyan 0.3°). Ötekiler ya erken koptu ya da kör dalışa girdi.

### Neden 5. geçişte, tek geçişte değil?

İlk 3 faz **20 m civarında** devralındı (20.8 / 20.4 / 10.1 m) — `GATE_MENZIL`
kapısı 20 m ve GPS fazı istasyonuna oturmadan kapı açılıyor. Karnede
görünüyor: **GPS istasyonunda oturma yalnızca %25.3**. Yani drone
istasyona (8-12 m bandına) girmeden kapıya değiyor, görsel faz hedefe
yetişemeyeceği menzilde devralıp hemen kaybediyor.

Vuran faz devri **11.7 m'de** aldı — tam istasyon bandında.

> Bu tam olarak `supervisor.py`'de yazılı olan çürütülmüş deneyin belirtisi:
> kapıyı gevşetmek sağlamlık üretmiyor, sağlam sanılan anları çoğaltıyor.

**Ayrıca:** GPS fazının kadraj yaw RMS'i **69.3°** — burun hedeften çok
sapıyor. Yeni entegrasyon (iç daire nişanı + `KD_H=0.60`) tam bu tabloyu
düzeltmeyi hedefliyor ama **bu uçuşta yoktu**.

---

## 3. "Vurduktan sonra hedef neden düşmüyor?"

**Sertlikle ilgisi yok.** Sistemde hasar modeli fiziksel değil, mantıksal:

```
Gazebo contact sensörü → gcs_server._hasar_uygula() → Talon'a force-disarm
```

Yani hedefin düşmesi çarpmanın şiddetine bağlı değil; disarm komutunun
gidip gitmediğine bağlı. Zincirin dört halkası var, biri koparsa hedef uçmaya
devam eder:

| halka | nerede | koparsa |
|---|---|---|
| Dünyada `gz-sim-contact-system` | `worlds/avci_harmonic.sdf:33` | sensör sessizce hiçbir şey yayınlamaz |
| Modelde `carpisma_sensoru` | `models/mini_talon_vtail/model.sdf:375` | temas topic'i hiç doğmaz |
| `AVCI_HASAR=1` + topic aboneliği | `gcs_server:880-953` | `[HASAR] temas topic'ine abone olunamadı` |
| `_plane_sysid` biliniyor olması | `gcs_server:903` | disarm komutu gönderilmez |

**Uçuşta hangisinin koptuğunu söylemek için Terminal B çıktısı gerekiyor.**
Bir sonraki uçuşta şu satırı ara:

```
[HASAR] GERÇEK çarpışma dinleniyor: /world/avci/model/mini_talon/.../contact
[HASAR] vuruş ölçütü = fiziksel temas (yakınlık TEK BAŞINA vuruş değil)
```

Bu satır **yoksa** temas hattı hiç kurulmamıştır → vuruş `yedek_menzil`
(1.5 m yakınlık) ile ilan edilmiştir ve hedefe hiçbir komut gitmemiştir.
Bu satır **varsa** ama hedef düşmüyorsa disarm halkası kopuktur.

> Log kanıtı: bu uçuşun `logs/plane_harmonic.log` dosyasında hiç `disarm`
> satırı yok. Ama dosya yalnız 2161 bayt ve 20:00'de yeniden başlatılmış,
> yani 17:51 uçuşunun kaydını taşımıyor — kesin hüküm veremiyoruz.

### Yapılacak

- [ ] Bir sonraki uçuşta Terminal B'de `[HASAR]` satırlarını kaydet.
- [ ] Vuruştan sonra: `gz topic -l | grep contact` → topic var mı?
- [ ] Vuruştan sonra Talon armed mı? (arayüz veya `[HASAR] Talon disarm`)

---

## 4. Karşılaştırma: benim halim vs Kayra'nın dalı

Merge-base `5bc5f8d` (5 Ağustos). İki dal oradan ayrıldı. **Kayra'nın dalından
YALNIZ GPS'e ait olanlar alındı.**

### 4a) ALINAN değerler

| ayar | benim halim | Kayra'nın dalı | şimdi | neden |
|---|---:|---:|---:|---|
| `KD_H` (lead) | 0.20 | **0.60** | **0.60** | menzil 34.3 → 29.4 m |
| `IC_KAYMA` (iç daire) | yok | **14.0 m** | **14.0 m** | menzil 34.1 → 9.8 m |
| `IC_ORAN` | yok | 0.0 (kapalı) | 0.0 | yarıçap-oranlı sürüm, rafta |
| `ISTASYON_ELEV_DEG` | 25.0° | **15.0°** | **15.0°** | `WP_ACC_Z` geri alınınca kendi koşulu gereği |
| `KP_H` | 0.8 | 0.8 | 0.8 | değişmedi |
| `V_MAX` | 18.0 | 18.0 | 18.0 | değişmedi (B8 ertelendi) |
| `RANGE_SET` | 11.0 | 11.0 | 11.0 | değişmedi |
| `VZ_MAX`/`MAX_ACCEL` | 6.0/12.0 | 6.0/12.0 | 6.0/12.0 | değişmedi |

### 4b) Araç parametreleri — DEĞER alındı, AD çevrildi

Kayra'nın dosyası olduğu gibi alındı (kullanıcı kararı), sonra adlar bu
makinenin firmware şemasına çevrildi. **Fiziksel değerler birebir aynı.**

| benim halim | Kayra'nın yazımı | şimdi (çevrilmiş) | fiziksel değer |
|---|---|---|---|
| `ATC_ANGLE_MAX 45` | `ANGLE_MAX 4500` | `ATC_ANGLE_MAX 45` | 45° — aynıydı |
| `WP_SPD 15` | `WPNAV_SPEED 2500` | **`WP_SPD 25`** | 15 → **25 m/s** |
| `WP_ACC 5` | `WPNAV_ACCEL 800` | **`WP_ACC 8`** | 5 → **8 m/s²** |
| `WP_SPD_UP 5` | `WPNAV_SPEED_UP 600` | **`WP_SPD_UP 6`** | 5 → **6 m/s** |
| `WP_SPD_DN 5` | `WPNAV_SPEED_DN 400` | **`WP_SPD_DN 4`** | 5 → **4 m/s** |
| `WP_ACC_Z 1` | `WPNAV_ACCEL_Z 250` | **`WP_ACC_Z 2.5`** | 1 → **2.5 m/s²** |
| — | `WPNAV_JERK 4` | **`WP_JERK 4`** | 1 → **4 m/s³** |
| `PSC_NE_VEL_P 2.0` | `PSC_VELXY_P 2.0` | `PSC_NE_VEL_P 2.0` | 2.0 — aynıydı |
| `LOIT_SPEED_MS 15` | — | *(çıkarıldı)* | Kayra'nın dosyasında yoktu |

> **Neden ad çevrildi:** `~/ardupilot/mav_5_1.parm` bu makinenin **tam**
> parametre dökümü (1421 satır, hiç set etmediğimiz `ATC_ANG_YAW_P`,
> `MOT_THST_HOVER` de içinde). Orada `^WPNAV` **0 satır**. ArduPilot tanımadığı
> adı sessizce yok sayar — çevrilmeseydi 8 satır uygulanmaz, araç firmware
> varsayılanlarıyla (10 m/s, 2.5 m/s², 30°) uçardı. Yani benim halimden de kötü.

### 4c) ALINMAYANLAR — bilinçli

| Kayra'nın dalında | neden alınmadı |
|---|---|
| `guidance_core.py` (+146 satır) | benim dalımda pose söküldü, dosya baştan yazıldı (313 satır değişti) |
| `visual_lead.py` (+96 satır) | aynı — bbox'a geçildi, B5 fly-past eklendi |
| `gcs_ui/script.js`, `style.css` | arayüz benim dalımda baştan yazıldı |
| `supervisor.py` — `get_gercek` parametresi | görsel faza ait |
| **GPS yaw kodu** | **Kayra'nın dalında hâlâ eski birikimli `cmd_yaw`.** Benim demirleme + süreli susma düzeltmem KORUNDU |
| `AVCI_POSE_KAYNAK=gercek` modu | pose tamamen kaldırıldı, karşılığı yok |
| "Görsel faz kapalı" ayarı | benim dalımda hibrit güdüm açık |

### 4d) ALINAN yeni modüller (varsayılan KAPALI)

| dosya | ne | nasıl açılır |
|---|---|---|
| `control/guidance/frpn.py` | FRPN güdüm yasası çekirdeği | — |
| `control/guidance/frpn_guidance.py` | FRPN uçuş döngüsü | `AVCI_GPS_GUDUM=frpn` |
| `control/guidance/hedef_kestirim.py` | IMM (CV+CA) kestirici | FRPN ile birlikte |
| `tools/frpn_replay.py` | katsayı taraması | `--tara` |
| `tools/zarf_olc.py` | araç zarfı ölçümü | — |
| `run_plane_scenario.py` | 4 daire çapı (⌀96/71/55/41) | `curl -X POST .../scenario/circle_s` |

FRPN uçuşta eski yasadan **iyi çıkmadı** (31.1 vs 29.4 m), o yüzden
varsayılan `istasyon`.

### 4e) gcs_server — gecikme düzeltmesi ALINDI

Kamera callback'i tüm işi senkron yapıyordu (medyan 21.8 ms) ama kamera 30 Hz
(bütçe 33.3 ms). Aşılınca gz-transport kuyruğu hiç boşalmıyordu. Telemetride
aynı hata: tek mesaj + 5 ms uyku = tavan 200 msg/s, oysa iki araç ~300-500
msg/s yayın yapıyor.

| | eski desen | yeni desen |
|---|---:|---:|
| ilk çeyrek gecikme | 80 ms | 19 ms |
| son çeyrek gecikme | **579 ms** | **19 ms** |

Bayat telemetri doğrudan GPS'in hedef hız kestirimini zehirlediği için bu
GPS'e ait sayıldı.

---

## 5. Sonuç — sıradaki iş

1. **Parametreleri doğrula:** SITL yeniden başlat → `python3 tools/parm_denetle.py`
   → COPTER **11/11 ✓** olmalı.
2. **Yeni tabanı ölç:** düz uçuş + `circle`. Bu belgedeki 12 fazlık tablo
   **eski** halin karnesi; yenisi henüz yok.
3. **`[HASAR]` satırlarını kaydet** — hedefin neden düşmediği ancak öyle
   kesinleşir.

Geri dönüş: `AVCI_GPS_KD=0.2 AVCI_GPS_IC=0 bash scripts/gcs.sh`
